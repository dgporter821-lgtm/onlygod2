[build]
  command = "npm run build"
  publish = "public"

# Edge Function Configuration
[[edge_functions]]
  function = "relay"
  path = "/*"

# Optional: Cache settings to reduce suspicion
[build.environment]
  NODE_VERSION = "18"
